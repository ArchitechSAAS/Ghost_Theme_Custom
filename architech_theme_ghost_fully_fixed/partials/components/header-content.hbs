
<header class="w-full py-4 border-b border-gray-200">
  <div class="max-w-7xl mx-auto px-4 flex justify-between items-center">
    <a href="{{@site.url}}" class="text-xl font-bold">{{@site.title}}</a>
    <nav class="space-x-6 text-sm">
      {{#foreach navigation}}
        <a href="{{url}}" class="hover:underline">{{label}}</a>
      {{/foreach}}
      <a href="/subscribe" class="bg-black text-white px-4 py-1 rounded-md">Subscribe</a>
    </nav>
  </div>
</header>
